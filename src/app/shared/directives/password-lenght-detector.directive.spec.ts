import { PasswordLenghtDetectorDirective } from './password-lenght-detector.directive';

describe('PasswordLenghtDetectorDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordLenghtDetectorDirective();
    expect(directive).toBeTruthy();
  });
});
