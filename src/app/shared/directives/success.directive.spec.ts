import { SuccessDirective } from './success.directive';

describe('SuccessDirective', () => {
  it('should create an instance', () => {
    const directive = new SuccessDirective();
    expect(directive).toBeTruthy();
  });
});
