import { FailureDirective } from './failure.directive';

describe('FailureDirective', () => {
  it('should create an instance', () => {
    const directive = new FailureDirective();
    expect(directive).toBeTruthy();
  });
});
